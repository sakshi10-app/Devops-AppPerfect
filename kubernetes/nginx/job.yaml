apiVersion: batch/v1
kind: Job
metadata:
  name: hello-job
  namespace: nginx
spec:
  completions: 1
  backoffLimit: 2
  template:
    spec:
      containers:
        - name: hello
          image: busybox
          command: ["echo", "Hello from Kubernetes Job"]
      restartPolicy: Never
